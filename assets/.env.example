# OpenAI / AI Configuration
# MOVED TO BACKEND: all AI calls (daily horoscope, chat, tarot) are now proxied
# through the Laravel backend (horora-api). The OpenAI API key stays server-side
# and must NOT be shipped in the client bundle.

# Telegram Bot Configuration
# MOVED TO BACKEND: merit order notifications are now sent server-side
# (horora-api). The bot token must NOT be shipped in the client bundle.

# OneSignal Push Notification
ONESIGNAL_APP_ID=your_onesignal_app_id_here
